package com.example.attemptservice.attempt.enums;

/** Shape of an implausible answer-timing pattern flagged after grading. */
public enum TimingAnomalyType {
    TOO_FAST_CORRECT,
    TOO_FAST_HIGH_SCORE,
    ZERO_TIME_CORRECT,
    BURST_SUBMIT
}
