package com.example.examservice.exam.enums;

/** Publication lifecycle of an exam definition. */
public enum ExamStatus {
    DRAFT,
    SCHEDULED,
    PUBLISHED,
    ACTIVE,
    CLOSED,
    ARCHIVED
}
