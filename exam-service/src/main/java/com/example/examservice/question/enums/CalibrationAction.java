package com.example.examservice.question.enums;

/** What a human should do about a question's calibration result. Never applied automatically. */
public enum CalibrationAction {
    KEEP,
    REVIEW,
    RETIRE,
    REWEIGHT
}
