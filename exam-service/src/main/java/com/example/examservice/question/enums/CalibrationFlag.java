package com.example.examservice.question.enums;

/** What a calibration run found wrong with a question, if anything. */
public enum CalibrationFlag {
    TOO_EASY,
    TOO_HARD,
    NEGATIVE_DISCRIMINATION,
    MIS_KEY_SUSPECTED,
    NONE
}
