package com.example.identityservice.auth.audit.enums;

/** Auditable operation class for the identity module. */
public enum AuditAction {
    CREATE,
    UPDATE,
    DELETE,
    LOGOUT,
    PERMISSION_CHANGE
}
