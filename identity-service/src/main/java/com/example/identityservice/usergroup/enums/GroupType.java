package com.example.identityservice.usergroup.enums;

/** Kind of student grouping an exam can be assigned to. */
public enum GroupType {
    CLASS,
    COHORT,
    DEPARTMENT,
    PROGRAM,
    CUSTOM
}
