package com.example.proctoringservice.proctoring.enums;

/** The candidate's own behavioral state, distinct from stream/connection health. */
public enum CandidateLiveStatus {
    ACTIVE,
    IDLE,
    SUSPENDED
}
