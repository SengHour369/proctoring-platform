package com.example.proctoringservice.proctoring.enums;

/** How a live proctor's shift on one session ended. */
public enum ProctorShiftOutcome {
    COMPLETED,
    HANDED_OVER,
    ESCALATED,
    ABANDONED
}
