package com.example.proctoringservice.sharedenums;

/** Level of supervision required while sitting an exam. */
public enum ProctoringMode {
    NONE,
    AI_ONLY,
    LIVE_PROCTOR,
    RECORD_AND_REVIEW,
    HYBRID
}
