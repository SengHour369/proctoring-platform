# Copy to .env (docker compose reads .env automatically) and fill in real values.
# Never commit the real .env — it's already in .gitignore.

REGISTRY=your-registry.example.com/proctoring-platform
TAG=1.0.0
