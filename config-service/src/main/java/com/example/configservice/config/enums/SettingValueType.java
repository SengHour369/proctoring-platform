package com.example.configservice.config.enums;

/** How a setting's stored string is to be parsed. */
public enum SettingValueType {
    STRING,
    INTEGER,
    DECIMAL,
    BOOLEAN,
    JSON,
    DURATION,
    ENUM
}
