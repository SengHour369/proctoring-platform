package com.example.test.attempt.enums;

/** Navigation state of one question within one attempt. */
public enum QuestionStateType {
    UNSEEN,
    VIEWED,
    ANSWERED,
    FLAGGED,
    SKIPPED,
    LOCKED
}
