package com.example.test.attempt.enums;

/** Per-answer grading progress; manual types start at PENDING. */
public enum GradingStatus {
    NOT_REQUIRED,
    PENDING,
    IN_PROGRESS,
    GRADED,
    REGRADED
}
