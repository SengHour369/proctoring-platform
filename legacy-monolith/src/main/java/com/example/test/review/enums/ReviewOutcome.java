package com.example.test.review.enums;

/** Settled result of a closed case. */
public enum ReviewOutcome {
    CLEARED,
    WARNING_ISSUED,
    SCORE_ADJUSTED,
    ATTEMPT_INVALIDATED,
    RETAKE_GRANTED,
    ESCALATED,
    NO_ACTION
}
