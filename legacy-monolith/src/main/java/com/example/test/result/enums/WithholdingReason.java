package com.example.test.result.enums;

/** Why a result is not yet releasable to the candidate. */
public enum WithholdingReason {
    RISK_THRESHOLD,
    OPEN_REVIEW_CASE,
    PENDING_MANUAL_GRADING,
    APPEAL_FILED,
    EXTERNAL_HOLD
}
